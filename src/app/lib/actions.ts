'use server';

/**
 * Server Actions removed in favor of Client-Side Firebase SDK.
 * This file is kept to maintain structural integrity but is no longer used for DB operations.
 */
export async function dummyAction() {
  return { success: true };
}
